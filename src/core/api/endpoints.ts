/**
 * API Endpoints & Route Constants
 * Strict isolation - Pure TypeScript.
 */

export const API_ENDPOINTS = {
  ANALYZE: '/api/analyze',
  DIFF: '/api/diff',
  CHAT: '/api/chat',
  PREP_KIT: '/api/prep-kit',
} as const;
